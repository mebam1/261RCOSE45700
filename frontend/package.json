{
  "name": "mvp1-cleanliness-mobile",
  "version": "0.1.0",
  "private": true,
  "description": "Downloadable mobile app wrapper for the MVP1 cleanliness capture frontend.",
  "scripts": {
    "build": "python scripts/build_app.py",
    "sync:android": "npm run build && cap sync android",
    "open:android": "npm run sync:android && cap open android",
    "build:android": "npm run sync:android && cd android && gradlew.bat assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.4",
    "@capacitor/core": "^7.4.4",
    "@capacitor/local-notifications": "^7.0.6"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.4"
  }
}
